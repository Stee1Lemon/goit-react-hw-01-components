import styled from 'styled-components';

export const Container = styled('div')(() => {
  return {
    margin: 30,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  };
});
